let add a b = a + b
let sub a b = a - b
let div a b = a / b
let mult a b = a * b
let pow a b = a ** b
let fact a = 
    if a = 0 then return 1
    elif x > 0 then return x * fact(x-1)
    else return printfn("Not a valid input to be able to factorial.")
